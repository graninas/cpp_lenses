Materials


1. `view` combinator.
2. `over`, `set` combinators.
3. Lens laws. Try also: L1 to L2 to IdL.
4. Generic LensStack. bindL template  problem.
5. Copy-paste problem of zoom which is bindL. Define will eliminate copy-paste.
6. `to` combinator. Proxy trick to obtain infix `to` operator. Type alias declarations, C++11. Left-associativity.
7. `traversed` combinator. Traversing of traversed.

Additional questions.
a. Generic lambda / function<> problem.
b. Can lenses manage undefined variables?
c. template template.

TODO
- Replace T parameters by const T& parameters (getters, setters, variators).
- Fold: Remove explicit containers `vector` etc.
- Generalize Fold & Lens.
- Remove brackets of lenses: addressL() -> addressl
- focus_type type is needed.
